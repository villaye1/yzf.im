# 像素派大星 · yzf.im

一个零依赖的静态单页网站，像素风派大星。

## 发布

当前使用 GitHub Pages，从 `main` 分支根目录发布。

自定义域名：`yzf.im`（CNAME 已配置）。

## 自定义

- 替换 `patrick-pixel.png`（或更新 index.html 里的图片）可更换角色。
- 修改 `app.js` 可换点击后的文案。
- 将 `index.html` 里的 GitHub 链接改成自己的主页。
